<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.StoRe" parent="Base.Theme.StoRe">
        <!--Material color attributes (dark theme) -->
        <item name="android:colorBackground">@color/theme_background_dark</item>
        <item name="colorOnBackground">@color/theme_on_background_dark</item>

        <item name="colorSurface">@color/theme_surface_dark</item>
        <item name="colorOnSurface">@color/theme_on_surface_dark</item>

        <!-- System styles -->
        <item name="android:windowLightNavigationBar" tools:ignore="NewApi">false</item>
    </style>
</resources>